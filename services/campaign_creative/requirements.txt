# IE3 — Campaign Creative Service
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
aiofiles>=23.0.0

# LLM — OpenRouter via OpenAI SDK
openai>=1.30.0

# HTTP client (Replicate REST API + async)
httpx>=0.27.0

# Database
psycopg[binary]>=3.1.0

# Environment variable loading
python-dotenv>=1.0.0

# Promotional image generation (Stable Diffusion fallback)
Pillow>=10.0.0

# Jinja2 fallback copy templates
jinja2>=3.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
