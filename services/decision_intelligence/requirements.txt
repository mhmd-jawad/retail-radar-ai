# IE2 — Decision Intelligence Service
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Feature engineering & validation
pandas>=2.2.0
pandera>=0.19.0
numpy>=1.26.0

# ML (v2 — install now, train when real outcome data available)
catboost>=1.2.5
shap>=0.45.0
mlflow>=2.12.0

# Observability
prometheus-client>=0.20.0

# Testing
pytest>=8.0.0
httpx>=0.27.0      # async test client for FastAPI
